apiVersion: v1
kind: Service
metadata:
  name: td-frontend
  namespace: traveldiscover
spec:
  selector:
    app: td-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
