apiVersion: v1
kind: Service
metadata:
  name: td-frontend-service
  namespace: default
spec:
  selector:
    app: td-frontend
  ports:
  - protocol: TCP
    port: 4200 # Expose the service on internal port 80
    targetPort: 4200 # Forward incoming requests to port 80
  type: ClusterIP
